name: GLM Lead · Astra Checkpoints
description: GLM 5.3 Flash leads and executes inline — root route follows the host/session model settings, execution workers pinned at max — with mandatory Astra (gpt-6-astra) checkpoints for architecture, final-diff review, and debugging consultations.
order: 10
